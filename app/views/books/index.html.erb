<h1>Book Catalogue</h1>
<ul>
  <% @books.each do |book| %>
    <li>
     <a href="/books/<%= book.id %>">
      <%= book.name %>
      </a>

    </li>
  <% end %>
</ul>

<%= link_to "New Book Entry", new_book_path %>